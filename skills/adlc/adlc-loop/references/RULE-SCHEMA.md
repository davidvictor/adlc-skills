# ADLC Loop Rule Schema

Reusable loop learnings should become rules only when they are repeated or high-impact.

```yaml
id: short-rule-id
trigger: when the rule should be considered
rule: concrete instruction
evidence: where the learning came from
owner: ADLC
status: proposed | active | retired
```

Do not turn one-off preferences into global rules without evidence.
